---
sidebar_position: 2
---

# opac

## opac.core

The `opac.core` module contains core classes and utilities.

## opac.helpers

The `opac.helpers` module provides helper functions to connect and interact with Yuzu.

## opac.models

The `opac.models` module contains model definitions used by `opac`.

## opac.utils

### opac.utils.fileManagement

The `opac.utils.fileManagement` module exposes file management helpers.

Note: the original reST used Sphinx `automodule` directives to document members. If you want the same level of API documentation in Docusaurus, consider generating API MD files or linking to an external API reference.
